#!/bin/bash

launch='docker run -i -t -v $(readlink -f $SSH_AUTH_SOCK):/ssh-agent -e SSH_AUTH_SOCK=/ssh-agent -h homeport --volumes-from homeport_home bigeasy/env zsh -l'

if which boot2docker > /dev/null; then
    boot2docker ssh -At sh -c "'""$launch""'"
else
    $launch
fi
